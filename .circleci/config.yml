version: 2.1
orbs:
  codecov: codecov/codecov@1.0.2
jobs:
  build:
    docker:
        - image: circleci/python:2.7.15
    steps:
      - checkout
      - run: python -m pip install --upgrade pip
      - run: sudo pip install -r requirements.txt
      - run: python -m pytest --junitxml=coverage.xml --cov=src tests/unit
      - codecov/upload:
          file: coverage.xml
